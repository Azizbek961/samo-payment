from django.urls import path
from . import views

urlpatterns = [
    # path('sozlamalar/', views.printer_settings, name='printer-settings'),
    # path('test/', views.test_print, name='printer-test'),
]